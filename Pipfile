# Legacy Pipenv metadata. The supported development workflow is uv; see pyproject.toml.
[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "fb2cal"

[packages]
mechanicalsoup = ">=1.3.0"
ics = ">=0.7.2"
requests = ">=2.31.0"
pycryptodomex = ">=3.20.0"
PyNaCl = ">=1.5.0"

[dev-packages]
freezegun = ">=1.5.0"
pytest = ">=8.0.0"
ruff = ">=0.11.0"
