[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fb2cal"
version = "2.0.0"
description = "Fetch Facebook birthdays and export calendar/contact files"
readme = "README.md"
requires-python = ">=3.10"
license = "GPL-3.0-only"
authors = [{ name = "Alessandro Digilio" }]
keywords = ["facebook", "birthday", "calendar", "export", "ics"]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Intended Audience :: Other Audience",
  "Topic :: Scientific/Engineering :: Information Analysis",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
]
urls = { Homepage = "https://github.com/alsd4git/fb2cal", Repository = "https://github.com/alsd4git/fb2cal", Upstream = "https://github.com/mobeigi/fb2cal" }
dependencies = [
  "MechanicalSoup>=1.3.0",
  "ics>=0.7.2",
  "requests>=2.31.0",
]

[project.scripts]
fb2cal = "fb2cal.__main__:main"

[tool.setuptools.packages.find]
include = ["fb2cal*"]

[dependency-groups]
dev = [
  "freezegun>=1.5.0",
  "pytest>=8.0.0",
  "ruff>=0.11.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
